/**
 * **logdy** - Web based real-time log viewer. Stream ANY content to a web UI with autogenerated filters. Parse any format with TypeScript.
 *
 * @domain `logdy.dev`
 * @programs `logdy`
 * @version `0.17.1` (15 versions available)
 * @versions From newest version to oldest.
 *
 * @install `launchpad install logdy.dev`
 * @homepage https://logdy.dev
 * @buildDependencies `go.dev@^1.21.4` - required only when building from source
 *
 * @example
 * ```typescript
 * import { pantry } from 'ts-pkgx'
 *
 * const pkg = pantry.logdydev
 * console.log(pkg.name)        // "logdy"
 * console.log(pkg.description) // "Web based real-time log viewer. Stream ANY cont..."
 * console.log(pkg.programs)    // ["logdy"]
 * console.log(pkg.versions[0]) // "0.17.1" (latest)
 * ```
 *
 * @see https://ts-pkgx.netlify.app/packages/logdy-dev.md
 * @see https://ts-pkgx.netlify.app/usage
 */
export const logdydevPackage = {
  /**
   * The display name of this package.
   */
  name: 'logdy' as const,
  /**
   * The canonical domain name for this package.
   */
  domain: 'logdy.dev' as const,
  /**
   * Brief description of what this package does.
   */
  description: 'Web based real-time log viewer. Stream ANY content to a web UI with autogenerated filters. Parse any format with TypeScript.' as const,
  packageYmlUrl: 'https://github.com/pkgxdev/pantry/tree/main/projects/logdy.dev/package.yml' as const,
  homepageUrl: 'https://logdy.dev' as const,
  githubUrl: 'https://github.com/logdyhq/logdy-core' as const,
  /**
   * Command to install this package using launchpad.
   * @example launchpad install package-name
   */
  installCommand: 'launchpad install logdy.dev' as const,
  pkgxInstallCommand: 'sh <(curl https://pkgx.sh) +logdy.dev -- $SHELL -i' as const,
  launchpadInstallCommand: 'launchpad install logdy.dev' as const,
  /**
   * Executable programs provided by this package.
   * These can be run after installation.
   */
  programs: [
    'logdy',
  ] as const,
  companions: [] as const,
  dependencies: [] as const,
  /**
   * Build dependencies for this package.
   * These are only required when building the package from source.
   */
  buildDependencies: [
    'go.dev@^1.21.4',
  ] as const,
  /**
   * Available versions from newest to oldest.
   * @see https://ts-pkgx.netlify.app/usage for installation instructions
   */
  versions: [
    '0.17.1',
    '0.17.0',
    '0.16.0',
    '0.15.0',
    '0.14.1',
    '0.14.0',
    '0.13.3',
    '0.13.2',
    '0.13.1',
    '0.13.0',
    '0.12.2',
    '0.12.0',
    '0.11.0',
    '0.10.0',
    '0.9.0',
  ] as const,
  aliases: [] as const,
}

export type LogdydevPackage = typeof logdydevPackage
